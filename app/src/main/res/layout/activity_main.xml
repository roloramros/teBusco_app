<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:openDrawer="start">

    <androidx.coordinatorlayout.widget.CoordinatorLayout
        android:id="@+id/main_content"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <!-- Toolbar Superior (Oculta pero necesaria para BaseActivity) -->
        <com.google.android.material.appbar.MaterialToolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@android:color/transparent"
            android:elevation="0dp"
            android:visibility="gone" />

        <!-- Mapa de Fondo -->
        <androidx.fragment.app.FragmentContainerView
            android:id="@+id/mapView"
            android:name="com.google.android.gms.maps.SupportMapFragment"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />

        <!-- Botón Menú Hamburguesa Flotante -->
        <com.google.android.material.card.MaterialCardView
            android:id="@+id/btnMenuContainer"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_margin="16dp"
            app:cardCornerRadius="24dp"
            app:cardElevation="8dp"
            app:strokeWidth="0dp"
            app:cardBackgroundColor="@color/white">

            <ImageButton
                android:id="@+id/btnMenu"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:src="@drawable/ic_menu"
                app:tint="@color/primary_dark_blue"
                android:scaleType="centerInside"
                android:padding="12dp"
                android:clickable="true"
                android:focusable="true"
                android:contentDescription="Abrir menú" />
        </com.google.android.material.card.MaterialCardView>

        <!-- Barra de Búsqueda Flotante -->
        <com.google.android.material.card.MaterialCardView
            android:id="@+id/searchBarContainer"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_margin="16dp"
            android:layout_marginTop="80dp"
            android:visibility="gone"
            app:cardCornerRadius="24dp"
            app:cardElevation="8dp"
            app:strokeWidth="0dp">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="48dp"
                android:orientation="horizontal"
                android:gravity="center_vertical"
                android:paddingHorizontal="16dp">

                <ImageView
                    android:layout_width="24dp"
                    android:layout_height="24dp"
                    android:src="@android:drawable/ic_menu_search"
                    app:tint="@color/text_secondary" />

                <TextView
                    android:id="@+id/tvSearchBar"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="12dp"
                    android:text="Buscar dirección..."
                    android:textColor="@color/text_secondary"
                    android:textSize="16sp" />
            </LinearLayout>
        </com.google.android.material.card.MaterialCardView>

        <!-- Panel Inferior Deslizable (Bottom Sheet) -->
        <androidx.core.widget.NestedScrollView
            android:id="@+id/bottomSheet"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/bg_bottom_sheet"
            android:elevation="16dp"
            app:behavior_peekHeight="120dp"
            app:layout_behavior="com.google.android.material.bottomsheet.BottomSheetBehavior">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:padding="20dp">

                <!-- Indicador de arrastre -->
                <View
                    android:layout_width="40dp"
                    android:layout_height="4dp"
                    android:layout_gravity="center"
                    android:layout_marginBottom="16dp"
                    android:background="#D0D0D0" />

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="¿Selección del Recorrido?"
                    android:textAppearance="@style/TextAppearance.Material3.TitleLarge"
                    android:textStyle="bold"
                    android:textColor="@color/primary_dark_blue" />

                <!-- Contenedor de Itinerario -->
                <com.google.android.material.card.MaterialCardView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="16dp"
                    app:cardCornerRadius="16dp"
                    app:cardElevation="0dp"
                    app:strokeWidth="1dp"
                    app:strokeColor="#E0E0E0">

                    <LinearLayout
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:orientation="vertical"
                        android:padding="12dp">

                        <!-- Origen -->
                        <com.google.android.material.textfield.TextInputLayout
                            android:id="@+id/tilOrigin"
                            style="@style/Widget.Material3.TextInputLayout.OutlinedBox"
                            android:layout_width="match_parent"
                            android:layout_height="wrap_content"
                            android:hint="Origen"
                            app:startIconDrawable="@android:drawable/presence_online"
                            app:startIconTint="@color/success_green"
                            app:endIconMode="custom"
                            app:endIconDrawable="@drawable/ic_star_outline">
                            <com.google.android.material.textfield.TextInputEditText
                                android:id="@+id/etOrigin"
                                android:layout_width="match_parent"
                                android:layout_height="wrap_content"
                                android:text="Mi ubicación actual"
                                android:focusable="false"
                                android:cursorVisible="false"
                                android:textSize="14sp" />
                        </com.google.android.material.textfield.TextInputLayout>

                        <!-- Contenedor Dinámico para Paradas (Aquí irán las paradas) -->
                        <LinearLayout
                            android:id="@+id/containerStops"
                            android:layout_width="match_parent"
                            android:layout_height="wrap_content"
                            android:orientation="vertical" />

                        <!-- Botón Añadir Parada -->
                        <com.google.android.material.button.MaterialButton
                            android:id="@+id/btnAddStop"
                            style="@style/Widget.Material3.Button.TextButton"
                            android:layout_width="wrap_content"
                            android:layout_height="wrap_content"
                            android:text="+ Añadir parada"
                            android:textColor="@color/primary_blue"
                            android:textSize="12sp" />

                        <!-- Destino -->
                        <com.google.android.material.textfield.TextInputLayout
                            android:id="@+id/tilDestination"
                            style="@style/Widget.Material3.TextInputLayout.OutlinedBox"
                            android:layout_width="match_parent"
                            android:layout_height="wrap_content"
                            android:layout_marginTop="4dp"
                            app:startIconDrawable="@android:drawable/presence_busy"
                            app:startIconTint="@color/error_red"
                            app:endIconMode="custom"
                            app:endIconDrawable="@drawable/ic_star_outline">
                            <com.google.android.material.textfield.TextInputEditText
                                android:id="@+id/etDestination"
                                android:layout_width="match_parent"
                                android:layout_height="wrap_content"
                                android:hint="Busca tu destino"
                                android:focusable="false"
                                android:cursorVisible="false"
                                android:textSize="14sp" />
                        </com.google.android.material.textfield.TextInputLayout>

                    </LinearLayout>
                </com.google.android.material.card.MaterialCardView>

                <LinearLayout
                    android:id="@+id/containerPublishButtons"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="horizontal"
                    android:layout_marginTop="20dp">

                    <com.google.android.material.button.MaterialButton
                        android:id="@+id/btnCancelRequest"
                        style="@style/Widget.Material3.Button.OutlinedButton"
                        android:layout_width="0dp"
                        android:layout_height="60dp"
                        android:layout_weight="1"
                        android:layout_marginEnd="12dp"
                        android:text="@string/btn_cancel"
                        app:cornerRadius="16dp" />

                    <com.google.android.material.button.MaterialButton
                        android:id="@+id/btnPublishRequest"
                        android:layout_width="0dp"
                        android:layout_height="60dp"
                        android:layout_weight="2"
                        android:text="@string/btn_confirm_route"
                        android:textAppearance="@style/TextAppearance.Material3.TitleMedium"
                        app:cornerRadius="16dp" />
                </LinearLayout>

            </LinearLayout>
        </androidx.core.widget.NestedScrollView>

        <!-- Pin Central para Selección de Ubicación -->
        <ImageView
            android:id="@+id/ivCentralPin"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_gravity="center"
            android:layout_marginBottom="40dp"
            android:src="@drawable/ic_location_pin"
            app:tint="@color/primary_blue"
            android:visibility="gone"
            android:elevation="20dp" />

        <!-- FAB de Mi Ubicación -->
        <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/fabMyLocation"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_margin="16dp"
            app:layout_anchor="@id/bottomSheet"
            app:layout_anchorGravity="top|end"
            android:contentDescription="Mi ubicación"
            android:src="@drawable/ic_my_location"
            app:backgroundTint="@color/white"
            app:tint="@color/primary_blue"
            android:elevation="20dp" />

        <!-- FAB de Confirmar Ubicación -->
        <com.google.android.material.floatingactionbutton.ExtendedFloatingActionButton
            android:id="@+id/fabConfirmLocation"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_margin="16dp"
            android:layout_gravity="bottom|end"
            android:layout_marginBottom="172dp"
            android:textColor="@color/white"
            app:icon="@android:drawable/ic_menu_compass"
            app:iconTint="@color/white"
            app:backgroundTint="@color/primary_blue"
            android:visibility="gone"
            android:elevation="20dp" />

    </androidx.coordinatorlayout.widget.CoordinatorLayout>

    <!-- Navigation View Original (para el menú lateral) -->
    <com.google.android.material.navigation.NavigationView
        android:id="@+id/nav_view"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        app:headerLayout="@layout/nav_drawer_header"
        app:menu="@menu/nav_drawer_menu"
        app:itemIconTint="@color/primary_blue"
        app:itemTextColor="@color/text_primary"
        app:itemIconPadding="20dp"
        app:itemHorizontalPadding="24dp">
        <include layout="@layout/nav_drawer_footer" />
    </com.google.android.material.navigation.NavigationView>

</androidx.drawerlayout.widget.DrawerLayout>